# 🚀 Развертывание Meskalivan на GitHub Pages (Из ветки)

## ✅ Что готово:
- ✅ Конфигурация Vite исправлена для корневого развертывания
- ✅ Создан файл 404.html для SPA
- ✅ Ключи Supabase встроены в код

## 🎯 Простое развертывание из ветки:

### Способ 1: Через GitHub CLI (рекомендуется)

1. **Соберите проект локально:**
```bash
npm install
npm run build
```

2. **Создайте и переключитесь на ветку gh-pages:**
```bash
git checkout -b gh-pages
```

3. **Скопируйте содержимое папки dist в корень:**
```bash
rm -rf * .gitignore .gitattributes
cp -r dist/* .
```

4. **Зафиксируйте и отправьте:**
```bash
git add .
git commit -m "Deploy to GitHub Pages"
git push origin gh-pages
```

5. **Включите GitHub Pages:**
   - Перейдите в Settings → Pages вашего репозитория
   - Source: "Deploy from a branch"
   - Branch: "gh-pages" → "/ (root)"
   - Нажмите "Save"

6. **Получите ссылку**: `https://vill300.github.io/meskalivan`

---

### Способ 2: Через GitHub (ручной)

1. **Перейдите в репозиторий**: https://github.com/Vill300/meskalivan
2. **Нажмите "Settings"** → **"Pages"**
3. **Источник**: "Deploy from a branch"
4. **Ветка**: "main" → **"/ (root)"**
5. **Нажмите "Save"**
6. **Подождите** пока появится ссылка

**ВНИМАНИЕ**: Этот способ работает только если в корне main ветки есть готовые HTML файлы. Рекомендуется использовать Способ 1.

---

## 🆘 Если что-то не работает:

### Проблема: 404 ошибка
- Убедитесь, что в ветке gh-pages есть файл index.html
- Проверьте, что в Settings → Pages выбрана правильная ветка

### Проблема: Сайт не загружается
- Подождите 5-10 минут после настройки
- Проверьте правильность ссылки

### Проблема: Файлы не найдены
- Убедитесь, что собрали проект командой `npm run build`
- Проверьте, что скопировали содержимое папки dist в корень ветки

---

## 🎮 Проверка работы:

После развертывания:

1. **Откройте ссылку**: https://vill300.github.io/meskalivan
2. **Должна появиться страница входа**
3. **Нажмите "Зарегистрироваться"**
4. **Создайте аккаунт** (email + пароль + имя пользователя)
5. **Войдите в систему**
6. **Увидите Discord-интерфейс** с каналами и чатом

**Готово! Ваш Discord-клон работает!** 🎉

---

## 📋 Альтернативы (если GitHub Pages не работает):

### Vercel (самый простой):
1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. "New Project" → выберите `meskalivan`
4. Deploy
5. Получите ссылку: `https://meskalivan-xxx.vercel.app`

### Netlify:
1. Перейдите на [netlify.com](https://netlify.com)
2. Войдите через GitHub
3. "New site from Git" → выберите `meskalivan`
4. Build: `npm run build`, Publish: `dist`
5. Deploy