# Инструкции по настройке GitHub и развертыванию

## 🚀 Загрузка на GitHub

### Вариант 1: Через GitHub CLI (рекомендуется)

1. Войдите в GitHub:
```bash
gh auth login
```

2. Создайте репозиторий и загрузите код:
```bash
gh repo create meskalivan --public --source=. --push
```

### Вариант 2: Вручную через веб-интерфейс

1. Перейдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий `meskalivan`
4. Сделайте его публичным
5. Не инициализируйте README (у нас уже есть)
6. Нажмите "Create repository"
7. Скопируйте URL репозитория
8. В терминале выполните:
```bash
git remote add origin https://github.com/YOUR_USERNAME/meskalivan.git
git push -u origin main
```

## 🌐 Настройка развертывания

### Вариант 1: Vercel (рекомендуется)

1. Перейдите на [Vercel](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Импортируйте репозиторий `meskalivan`
5. Vercel автоматически определит настройки Vite
6. Нажмите "Deploy"
7. Получите ссылку на сайт

### Вариант 2: Netlify

1. Перейдите на [Netlify](https://netlify.com)
2. Войдите через GitHub
3. Нажмите "New site from Git"
4. Выберите GitHub и репозиторий `meskalivan`
5. Настройки сборки:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. Нажмите "Deploy site"

### Вариант 3: GitHub Pages

1. В репозитории перейдите в Settings → Pages
2. Выберите Source: "GitHub Actions"
3. Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm install
    
    - name: Build
      run: npm run build
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
```

4. Зафиксируйте и отправьте изменения
5. Дождитесь завершения деплоя

## ⚙️ Настройка переменных окружения

### Для Vercel:
1. В панели Vercel перейдите в Settings → Environment Variables
2. Добавьте:
   - `VITE_SUPABASE_URL`: ваш Supabase URL
   - `VITE_SUPABASE_ANON_KEY`: ваш Supabase anon key

### Для Netlify:
1. В панели Netlify перейдите в Site settings → Environment variables
2. Добавьте те же переменные

### Для GitHub Pages:
Добавьте переменные в настройки репозитория → Secrets and variables → Actions

## 🔗 Получение ссылки

После развертывания вы получите ссылку вида:
- Vercel: `https://your-project.vercel.app`
- Netlify: `https://random-name.netlify.app`
- GitHub Pages: `https://your-username.github.io/meskalivan`

## 🛠️ Устранение проблем

### Ошибки сборки
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте правильность путей в конфигурации

### Ошибки Supabase
- Проверьте правильность ключей API
- Убедитесь, что URL и ключи корректны
- Проверьте настройки аутентификации в Supabase

### Ошибки развертывания
- Проверьте логи сборки в панели хостинга
- Убедитесь, что команда сборки и директория публикации настроены правильно