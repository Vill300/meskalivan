# 🔧 ИСПРАВЛЕНИЕ ОШИБКИ 404 НА GITHUB PAGES

## ❌ Проблема: Ошибка 404 на https://vill300.github.io/meskalivan

## 🔍 Диагностика:

### Шаг 1: Проверьте, что файлы загружены
1. **Перейдите в репозиторий**: https://github.com/Vill300/meskalivan
2. **Проверьте ветки** - нажмите на выпадающий список веток (где написано "main")
3. **Если видите ветку `gh-pages`** - переключитесь на нее
4. **Проверьте содержимое** - должен быть файл `index.html` в корне

### Шаг 2: Если ветки gh-pages нет или она пуста

#### Способ A: Запустите скрипт (РЕКОМЕНДУЕТСЯ)
```bash
# В вашем локальном проекте
chmod +x deploy.sh
./deploy.sh
```

#### Способ B: Загрузите вручную
1. **Соберите проект локально:**
```bash
npm install
npm run build
```

2. **Создайте ветку gh-pages:**
```bash
git checkout -b gh-pages
```

3. **Удалите все файлы из корня** (кроме .git):
```bash
# Удалите все файлы и папки, кроме .git
```

4. **Скопируйте файлы из папки dist:**
```bash
cp -r dist/* .
```

5. **Загрузите в GitHub:**
```bash
git add .
git commit -m "Deploy to GitHub Pages"
git push origin gh-pages
```

### Шаг 3: Настройте GitHub Pages правильно
1. **Перейдите**: https://github.com/Vill300/meskalivan/settings/pages
2. **Источник**: "Deploy from a branch" 
3. **Ветка**: "gh-pages" → "/ (root)"
4. **Нажмите "Save"**

### Шаг 4: Проверьте настройки
1. **Дождитесь сообщения** "Your site is ready to be published"
2. **Ссылка должна появиться**: `https://vill300.github.io/meskalivan`

---

## 🚨 Если проблема не решается:

### Проверьте настройки репозитория:
1. **Перейдите**: https://github.com/Vill300/meskalivan
2. **Нажмите "Settings"** (вверху)
3. **Прокрутите до "Pages"** (левое меню)
4. **Источник должен быть**: "Deploy from a branch"
5. **Ветка**: "gh-pages" → "/ (root)"

### Проверьте файлы в ветке gh-pages:
1. **Переключитесь на ветку** `gh-pages`
2. **Должны быть файлы**:
   - `index.html` (главная страница)
   - `assets/` (папка с CSS/JS)
   - `404.html` (для SPA)

---

## 🔄 Быстрое решение через Vercel (АЛЬТЕРНАТИВА):

Если GitHub Pages не работает, используйте Vercel:

1. **Перейдите**: https://vercel.com
2. **Войдите через GitHub**
3. **"New Project"** → выберите `meskalivan`
4. **"Deploy"**
5. **Получите ссылку**: `https://meskalivan-xxx.vercel.app`

---

## ✅ Что должно работать после исправления:

1. **Ссылка загружается**: https://vill300.github.io/meskalivan
2. **Появляется страница входа**
3. **Можно зарегистрироваться и войти**
4. **Отображается интерфейс Discord-клона**

## 🆘 Если все еще не работает:

1. **Подождите 10-15 минут** (иногда нужно время для распространения)
2. **Очистите кэш браузера** (Ctrl+F5)
3. **Попробуйте в режиме инкогнито**
4. **Проверьте в другом браузере**

**После выполнения всех шагов ошибка 404 должна исчезнуть!** 🎉